Before version 2.0 will be released:
    SFTP support
    FTP / Configs compatibility with versions >= 2.3.5
    Nginx / Ruby+RoR
    Nginx / Proxy-pass hosts

Sometime:
    If main domain was removed, sub domain can't be removed.
    List of all created websites/domains/ftp accounts & etc.
    Auto detect - cpu, memory & etc
    Tests for modules: backups, certs, open vpn, web-interface
    Complex add/remove hosts
